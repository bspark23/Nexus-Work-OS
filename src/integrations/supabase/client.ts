/**
 * DEPRECATED — Supabase has been replaced by Firebase.
 * This file is kept temporarily to avoid import errors during transition.
 * All references should be updated to use:
 *   import { auth, db, storage } from "@/integrations/firebase/config"
 */
export {};
